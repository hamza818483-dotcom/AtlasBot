name: Sync to Hugging Face Space (DISABLED - HF accounts banned)

# HF deploy disabled. Render is the live host.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "HF deploy disabled"
